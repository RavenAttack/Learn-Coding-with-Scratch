/**
 * Placeholder C++ generator.
 * Later: map Blockly blocks to C++ syntax.
 */
export function generateCpp(_workspace) {
  return [
    '// C++ generator is a placeholder for now.',
    '// Future work: convert custom game blocks to C++ code.',
  ].join('\n');
}
