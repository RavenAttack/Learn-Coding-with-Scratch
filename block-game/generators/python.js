/**
 * Placeholder Python generator.
 * Later: map Blockly blocks to Python syntax.
 */
export function generatePython(_workspace) {
  return [
    '# Python generator is a placeholder for now.',
    '# Future work: convert custom game blocks to Python code.',
  ].join('\n');
}
